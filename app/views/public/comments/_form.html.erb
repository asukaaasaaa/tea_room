<div class="new-comment card-text">
  <%= form_with(model:[post_tea, comment], local: false) do |f| %>
    <div class="form-group">
      <label for="exampleFormControlTextarea1">-コメント-</label>
      <%= f.text_area :comment, rows:'5', class: "form-control comment-textarea",placeholder: "コメントをここに" %>
    </div>
    <%= f.submit "送信する", class:"btn btn-lg btn-info mt-2 float-right" %>
  <% end %>
</div>